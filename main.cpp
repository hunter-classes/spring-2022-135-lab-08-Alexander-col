#include <iostream> 
#include "imageio.h"
int main()
{
    std::cout << "Is this truly the end of all there is to come?" << std::endl;
    int out[MAX_H][MAX_W];
    int w, h;
    readImage("image1.pgm", out, h, w);
    int out[MAX_H][MAX_W];


}